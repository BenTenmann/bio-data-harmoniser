const statusColors = {
  running: "cyan",
  success: "lime",
  failed: "rose",
};

export { statusColors };

export enum QueryStatus {
  RUNNING = "running",
  SUCCESS = "success",
  FAILED = "failed",
}
